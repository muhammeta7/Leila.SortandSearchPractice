package strings;

public class SortStringArrayOfIntegers {


    // Given: an array of numbers but stored as strings.
    // Task: Sort them in ascending order
    // Example: instead of {9, 8} you have {“9”, “8”, “15”}
    // {“5”, “33333333333333”}

    //public String[] sortTheseNumbers(String[] arr){
    //
    //      your code here
    //		Int n = arr.legth();
    //		for(int i = 0; i < n ; i++){
    //		for(int j =0; j < n-i-1; j++){
    //		if(arr[j].compareTo(arr[j+1) > 0 && arr[j].length() == arr[j+1].length()){
    //		String temp = arr[j];
    //		Arr[j] = arr[j+1];
    //		Arr[j+1] = temp;
    //		Else if(arr[j].length() > arr[j+1].length() {
    //		String temp = arr[j];
    //		Arr[j] = arr[j+1];
    //		Arr[j+1] = temp;
    //		}
    //		}
    //		Return arr;
}
